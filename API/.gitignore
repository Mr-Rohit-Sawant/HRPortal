# Dependencies
node_modules/

# Build output (committed so server doesn't need to compile)
# dist/

# Environment variables (NEVER commit this)
.env
.env.local
.env.production

# Uploads (user files - don't commit)
uploads/*
!uploads/.gitkeep

# Logs
logs/
*.log

# OS files
.DS_Store
Thumbs.db

# TypeScript cache
*.tsbuildinfo
